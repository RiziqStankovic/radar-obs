# Domain docs

This is a single-context repository.

Before exploring a domain area:

1. Read `CONTEXT.md` at the repository root if it exists.
2. Read relevant ADRs under `docs/adr/` if they exist.
3. Use the terminology defined by the context document and ADRs.

Domain documents are created lazily when a domain decision is resolved; their absence is not itself a problem.
